# Python package requirements for the reproduction scripts.
# Verified with Python 3.10+ and the versions pinned below; nearby versions
# should also work.
pandas>=2.0
numpy>=1.24
statsmodels>=0.14
matplotlib>=3.7
